script that uses cryptography fernet to encrypt and decrypt files
and to generate a randon key anda  decryption file with a passwd
  and it also uses hashlib to make the input code attempt to get hashed (sha-256) and compare with
  the hashed string variable in the code.



  dont use for unethical stuff 🐓
